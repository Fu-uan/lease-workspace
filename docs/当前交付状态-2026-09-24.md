# 当前交付状态（2026-09-24）

## 本轮已完成

- 合同录入改为“先基本信息、再费用与计费段、最后复核提交”的顺序；合同编号置于首项。
- 甲方、实际付款方、实际收款方分列，避免扫描结果把出租方和收款方混为一项。
- 识别候选、规则推算、人工修改分别标记；金额明细和字段可展开查看原值。
- 草稿可按登录账号在当前浏览器恢复；服务端保存来源快照，重试不会重复建卡。
- 提交、退回、撤回、重新提交、审批和付款审批均写入不可变事件时间线；审批通过不自动伪造“已付款”。
- 增加本地 SQLite 适配器，业务层通过既有适配器接口访问数据，为迁移到公司数据库保留替换边界。
- 本地闭环测试 65 项通过，包含重复提交、退回重提、撤回、付款批次幂等、实际付款重复登记保护。

## 当前仍需在公司环境确认

- Demo 已增加 OpenAI 兼容的 AI 扫描代理：页面可配置接口地址、模型名和 API Key，默认地址为公司中转站路径。Key 仅随当前请求传给后端，不写入源码、业务数据或日志；返回结果必须先进入人工核对。
- 正式环境仍需用公司网关实际验证认证头、请求体和多模态返回格式；若网关策略不同，只需调整后端代理，不把 Key 写入前端。
- 公司正式数据库、对象存储、身份认证（企业微信或其他 SSO）以及网关部署方式尚未提供，因而尚不能宣称生产迁移已经完成。
- Demo 上传边界已调整为单文件 50MB、PDF 最多 50 页，并通过 `ZL_UPLOAD_MAX_MB`、`ZL_PDF_MAX_PAGES` 可配置。正式版仍应由服务器配置可审计的大小、页数、MIME、病毒扫描和对象存储策略，不能简单取消边界。
- 本地适配器只证明业务层可替换存储，不等于已完成公司服务器的事务、备份、权限和灾备方案。

## 交付给开发的迁移边界

保留 `workflow.py` 的业务动作和事件结构，替换 `local_store.py` 为公司数据库适配器；把文件服务替换为公司对象存储；把 `find_user`/会话替换为公司 SSO；将模型调用收敛到后端 `/api/ocr` 代理。页面只依赖业务 API，不直接依赖 WorkBuddy 表格或团队空间字段。迁移验收必须逐项回放本地闭环，并核对写入回读、权限隔离和审计时间线。
