# 租赁线上化 · 轻应用交付说明

> 架构：**账号密码登录 + 团队空间 database 存储 + token 回填 + 审批状态机 + OpenProperty UI**
> 目录：`D:\workbuddy\财务租赁\租赁线上化LightApp\`

---

## 1. 是什么

把「租赁线上化 demo」升级为**可运行、带鉴权、带审批闭环**的轻应用，数据落库到 WorkBuddy 资料库团队空间（非本地 SQLite），前端用 OpenProperty/shadcn 风格。

闭环：**前端页面登录 → 维护人建合同/提交 → 审批人在线通过/退回 → token 回填团队空间 database → 页面实时回读**。

## 2. 文件清单

| 文件 | 作用 |
|---|---|
| `backend.py` | 可发布 HTTP 后端（监听 `0.0.0.0:$PORT`）：登录鉴权、角色权限、审批状态机、防重/防并发、token 回填 |
| `library_client.py` | 直连 WorkBuddy 资料库 REST 客户端（query/add/update，`X-Skill-Token`） |
| `index.html` | OpenProperty 风格单文件前端（登录页 + 待办审批 + 合同台账 + 详情 + 新建 + 审批弹窗） |
| `config.json` | 空间 / 表 ID、角色、状态机、加密盐 |
| `test_http.py` | HTTP 层端到端验证：三角色登录 + 越权拦截 + 防重复 + 完整审批闭环 |

## 3. 团队空间数据库（已创建）

空间：财务团队（卓越）`JBkfhBQHmJh3KBuCievFcv`

| 表 | database ID | 用途 |
|---|---|---|
| ZL_租赁用户 | `vDEPC5UibUUBW13YialDV8` | 账号密码哈希 / 姓名 / 角色 / 启用 |
| ZL_合同审批 | `JCBGLWua9JnTDxZnohOD7k` | 合同卡片 + 审批状态机 + 提交人/审批人/意见 |
| ZL_审批记录 | `Yiiy10isMMJczU3eAjQ4kM` | 审计流水：提交/通过/退回/重提 |

> 与 demo 的「租赁合同台账 / 租金台账」并列新增，互不污染；轻应用用 ZL_* 三表。

## 4. 初始账号（可改）

| 账号 | 密码 | 姓名 | 角色 |
|---|---|---|---|
| `admin` | `admin123` | 系统管理员 | 管理员 |
| `approver` | `approver123` | 张审批 | 审批人 |
| `keeper` | `keeper123` | 李维护 | 维护人 |

> 密码采用 `sha256(salt+密码)` 单次哈希入库，salt 在 `config.json`。正式使用建议换强密码。

## 5. 角色与权限（服务端强制，前端藏按钮 + 后端拦接口）

| 动作 | 管理员 | 审批人 | 维护人 |
|---|---|---|---|
| 新建合同（草稿） | ✅ | ❌ | ✅ |
| 提交审批 | ✅ | ❌ | ✅ |
| 审批：通过 | ✅ | ✅ | ❌ |
| 审批：退回 | ✅ | ✅ | ❌ |
| 重提 | ✅ | ❌ | ✅ |
| 查看台账/详情/审批记录 | ✅ | ✅ | ✅ |

越权调用接口一律 `403`（服务端 `_require` 校验角色）。

## 6. 审批状态机（防重/防并发）

```
草稿 --提交--> 待审 --通过--> 通过
                  --退回--> 退回 --重提--> 待审
```

- **状态机校验**：只有 `待审` 可审批，只有 `退回` 可重提，只有 `草稿` 可提交；否则服务端拒绝。
- **全局互斥锁**：同一合同并发双击 / 双窗口，第二次直接拒绝，浏览器无法旁路。
- **审计留痕**：每次动作写「审批记录」表（动作/操作人/时间/意见）。

## 7. 端到端验证结果（已通过）

HTTP 层真实服务 + 三角色登录，全部断言通过：

- 登录三角色 ✓
- 维护人新建 → 草稿 ✓
- 维护人提交 → 待审 ✓
- **越权拦截：维护人调审批接口 → 403** ✓
- 审批人通过 → 通过 ✓
- **防重复：已通过再审批 → 拒绝** ✓
- 已通过合同从待办消失 ✓
- 管理员退回 → 维护人重提 → 待审 ✓
- 审批记录落库（提交/通过）✓

## 8. 本地运行 / 发布

发布上线 ✅（2026-09-21）：
- **分享链接**：`https://lease-ifrs16-calc.app.workbuddy.host/`
- 覆盖了工作区旧应用「租赁管理轻应用」的链接（保留原域名）。
- 管理：前往「设置—数据管理—应用」中管理。

本地调试：
```bash
export ZL_TOKEN='<资料库token>'
python backend.py 8799        # http://127.0.0.1:8799
```

**发布环境鉴权要点（重要，已踩坑解决）**：
- 发布沙箱 `X_IDE_IS_CLOUDSTUDIO=true` → 走 auth-proxy 访问资料库，**无需自带 token**（`backend.py` 已在 `is_sandbox()` 分支自动处理）。
- **发布平台会占用 `Authorization` header 承载平台自己的 JWT**，应用级鉴权 token **绝不能**走该 header，否则必被覆盖导致 401。应用 token 只走 `body._token` / `url?token=`（前端 `API.req` 已实现；后端 `current()` 已忽略 header、只读 body/query）。
- 会话用**无状态 HMAC 签名 token**（固定密钥），天然跨多实例/多容器可用，无共享内存依赖。

## 9. 需求满足度（对照两份需求文件）

- **48 项**天然满足（台账/审批/结算方式/自然月算法/IFRS 复核/凭证导出等主干）。
- **17 项**需外部数据源或后续接系统（OA 自动入池、AI 抽取强校验、银行回写等）。
- **5 项**本质不满足（OA 授权、真实账务、银行回写、外部效能数据、推送式实时）。

详见会话中的《轻应用方案需求满足度核对》。
