# 测试报告

## 本轮追加验证

- 本地隔离测试 `test_core.py test_settlement.py tests/test_overview_readback.py`：26 passed。新增用例覆盖记录ID兼容、逐字段回读不一致时保留旧数据、缺资料的已通过合同不能显示所有步骤完成。
- JavaScript 语法检查、git diff --check 通过。
- 对用户指定的 `C:\Users\13777\Desktop\ocr_test_contract.png` 执行真实本地 OCR：20.94 秒，2120 字符，提取付款表 8 行。
- 对 `C:\Users\13777\Desktop\示例合同扫描件.png` 执行真实本地 OCR：3.44 秒，264 字符，提取付款表 0 行。未核对每个字段与原图的一致性，不能宣称识别准确率通过。
- 两张图片尚未完成浏览器上传、保存、人工确认、审批和团队空间回读的完整闭环。
- 上述 OCR 耗时不是首页或侧边栏耗时。未完成浏览器性能复测。
- 更正：此前将 test_review_flow.py 放入 pytest 命令，不代表其 main() 中的真实库端到端流程已执行；23 passed 仅可作为被收集用例的结果。
- 当前金额概览新增逐字段回读校验；付款计划中的应付日、租金/管理费拆分等完整明细尚未新增持久化接口，不应将金额概览保存称为完整付款计划保存。

## 本地回归

- `test_core.py test_review_flow.py test_settlement.py`：23 passed。
- OCR 全量回归仍有 3 个既有乱码边界用例失败，涉及 `test_ocr_extract.py` 的冲突值、无冒号面积和无冒号月租金提取；本次前端付款计划修复未修改这些提取规则。
- `test_http.py` 未能完成收集，测试账号登录返回 WorkBuddy 接口 401/502，属于外部授权状态问题。

## 浏览器验证

- 已登录角色：张维护（台账维护人）。
- 已打开“新建租赁卡片”并检查字段、识别入口、状态和保存按钮。
- 手动添加扫描件后确认：基础字段能够回填，付款计划摘要显示 5 段，但旧线上版本明细表显示 0 期；本次代码已修复该映射。
- AI 扫描旧入口实际为 Prompt/JSON 导入桥接，本次已改名并补充能力边界文案。
- 未点击保存、提交审批、删除或付款确认。
