# WorkBuddy 发布提示词

请用本目录最新代码发布同一轻应用，并保留现有数据、账号、角色和团队空间配置。

本次发布重点验证：

1. 新建租赁卡片中上传合同并点击“识别”。
2. OCR 返回付款计划后，识别的每一段都必须出现在“付款计划（N 期，可编辑）”表格中，N 不能为 0。
3. “导入外部 AI 结果”只作为辅助入口，页面文案不得暗示系统内置 AI 模型。
4. 选择“是否 IFRS=是”且不填写折现率时，保存必须被阻止并提示原因。
5. 识别结果必须保持“已识别待核对”，人工确认前不得显示为已确认。
6. 保留“打开团队空间”入口及原有链接，不删除现有表和记录。

发布后请返回：版本号、发布时间、实际访问链接、是否保留原数据、上述 6 项验证结果。
